version: "3.8"
services:
  foxbrain-rag:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./documents:/app/documents
      - ./data:/app/data
    restart: unless-stopped
